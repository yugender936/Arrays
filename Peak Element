class Solution {

    public int peakElement(int[] arr) {
        // code here
        int i = 0;
       
       while(i < arr.length - 1 && arr[i] < arr[i+1]){
           i++;
       }
       
       return i;
        
    }
}
