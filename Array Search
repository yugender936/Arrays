class Solution {

    static int search(int arr[], int x) {

        // Your code here
        int ele = -1;
        for(int i = arr.length - 1 ; i >= 0 ; i--){
            if(arr[i] == x){
                ele = i;
            }
        }
        return ele;
    }
}
